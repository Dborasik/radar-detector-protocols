# RAD 700i examples

No fabricated packet examples are included.

Add examples only when they are derived from a cited capture or reproducible experiment. Each example should state:

- raw bytes
- transport/characteristic
- detector firmware if known
- triggering action
- decoded fields
- confidence for each interpretation
- source/capture ID
